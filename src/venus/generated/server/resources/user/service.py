# This file was auto-generated by Fern from our API Definition.

import abc
import functools
import inspect
import logging
import typing

import fastapi

from ...core.abstract_fern_service import AbstractFernService
from ...core.exceptions.fern_http_exception import FernHTTPException
from ...core.route_args import get_route_args
from ...security import ApiAuth, FernAuth
from .types.organizations_page import OrganizationsPage
from .types.user import User


class AbstractUserService(AbstractFernService):
    """
    AbstractUserService is an abstract class containing the methods that your
    UserService implementation should implement.

    Each method is associated with an API route, which will be registered
    with FastAPI when you register your implementation using Fern's register()
    function.
    """

    @abc.abstractmethod
    def get_myself(self, *, auth: ApiAuth) -> User:
        ...

    @abc.abstractmethod
    def get_my_organizations(self, *, page_id: int, auth: ApiAuth) -> OrganizationsPage:
        ...

    @abc.abstractmethod
    def belongs_to_organization(self, *, organization_id: str, auth: ApiAuth) -> bool:
        ...

    """
    Below are internal methods used by Fern to register your implementation.
    You can ignore them.
    """

    @classmethod
    def _init_fern(cls, router: fastapi.APIRouter) -> None:
        cls.__init_get_myself(router=router)
        cls.__init_get_my_organizations(router=router)
        cls.__init_belongs_to_organization(router=router)

    @classmethod
    def __init_get_myself(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.get_myself)
        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "auth":
                new_parameters.append(parameter.replace(default=fastapi.Depends(FernAuth)))
            else:
                new_parameters.append(parameter)
        setattr(cls.get_myself, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.get_myself)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> User:
            try:
                return cls.get_myself(*args, **kwargs)
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'get_myself' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        # this is necessary for FastAPI to find forward-ref'ed type hints.
        # https://github.com/tiangolo/fastapi/pull/5077
        wrapper.__globals__.update(cls.get_myself.__globals__)

        router.get(
            path="/users",
            response_model=User,
            description=cls.get_myself.__doc__,
            **get_route_args(cls.get_myself, default_tag="user"),
        )(wrapper)

    @classmethod
    def __init_get_my_organizations(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.get_my_organizations)
        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "page_id":
                new_parameters.append(parameter.replace(default=fastapi.Query(default=..., alias="pageId")))
            elif parameter_name == "auth":
                new_parameters.append(parameter.replace(default=fastapi.Depends(FernAuth)))
            else:
                new_parameters.append(parameter)
        setattr(cls.get_my_organizations, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.get_my_organizations)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> OrganizationsPage:
            try:
                return cls.get_my_organizations(*args, **kwargs)
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'get_my_organizations' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        # this is necessary for FastAPI to find forward-ref'ed type hints.
        # https://github.com/tiangolo/fastapi/pull/5077
        wrapper.__globals__.update(cls.get_my_organizations.__globals__)

        router.get(
            path="/users/organizations",
            response_model=OrganizationsPage,
            description=cls.get_my_organizations.__doc__,
            **get_route_args(cls.get_my_organizations, default_tag="user"),
        )(wrapper)

    @classmethod
    def __init_belongs_to_organization(cls, router: fastapi.APIRouter) -> None:
        endpoint_function = inspect.signature(cls.belongs_to_organization)
        new_parameters: typing.List[inspect.Parameter] = []
        for index, (parameter_name, parameter) in enumerate(endpoint_function.parameters.items()):
            if index == 0:
                new_parameters.append(parameter.replace(default=fastapi.Depends(cls)))
            elif parameter_name == "organization_id":
                new_parameters.append(parameter.replace(default=fastapi.Path(...)))
            elif parameter_name == "auth":
                new_parameters.append(parameter.replace(default=fastapi.Depends(FernAuth)))
            else:
                new_parameters.append(parameter)
        setattr(cls.belongs_to_organization, "__signature__", endpoint_function.replace(parameters=new_parameters))

        @functools.wraps(cls.belongs_to_organization)
        def wrapper(*args: typing.Any, **kwargs: typing.Any) -> bool:
            try:
                return cls.belongs_to_organization(*args, **kwargs)
            except FernHTTPException as e:
                logging.getLogger(f"{cls.__module__}.{cls.__name__}").warn(
                    f"Endpoint 'belongs_to_organization' unexpectedly threw {e.__class__.__name__}. "
                    + f"If this was intentional, please add {e.__class__.__name__} to "
                    + "the endpoint's errors list in your Fern Definition."
                )
                raise e

        # this is necessary for FastAPI to find forward-ref'ed type hints.
        # https://github.com/tiangolo/fastapi/pull/5077
        wrapper.__globals__.update(cls.belongs_to_organization.__globals__)

        router.get(
            path="/users/belongs-to-organization/{organization_id}",
            response_model=bool,
            description=cls.belongs_to_organization.__doc__,
            **get_route_args(cls.belongs_to_organization, default_tag="user"),
        )(wrapper)
